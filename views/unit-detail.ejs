<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <title><%= unit.name %> | Unit Intel</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="app-container">
        <%- include('partials/navbar') %>

        <main class="main-content">
            <a href="/units" class="btn-back">&larr; Terug naar divisies</a>

            <div class="detail-card" style="border-top: 4px solid var(--gold);">
                <div class="detail-image-container">
                    <img src="<%= unit.emblemUrl %>" alt="Emblem" style="background: rgba(255,255,255,0.05); padding: 20px;">
                </div>

                <div class="detail-info">
                    <span class="rank-badge" style="color: white; background: var(--bg-dark);">UNIT ID: <%= unit.id %></span>
                    <h2 style="color: var(--gold);"><%= unit.name %></h2>
                    
                    <div style="margin: 20px 0; font-style: italic; border-left: 2px solid var(--text-muted); padding-left: 15px; font-size: 1.2rem;">
                        "<%= unit.motto %>"
                    </div>

                    <div class="info-grid">
                        <div class="info-item">
                            <label>Classificatie</label>
                            <span><%= unit.isElite ? 'ELITE TIER 1' : 'STANDARD INFANTRY' %></span>
                        </div>
                         <div class="info-item">
                            <label>Opgericht</label>
                            <span><%= unit.foundedYear %></span>
                        </div>
                    </div>
                    
                    <h3 style="margin-top: 30px; border-bottom: 1px solid var(--border); padding-bottom: 10px;">Active Operators</h3>
<ul class="unit-members-list" style="list-style: none; padding: 0; margin-top: 15px;">                        <% members.forEach(member => { %>
                            <li style="margin-bottom: 10px;">
                                <a href="/characters/<%= member.id %>" style="color: white; text-decoration: none; display: flex; align-items: center; gap: 10px; background: var(--bg-dark); padding: 10px; border-radius: 5px;">
                                    <img src="<%= member.imageUrl %>" style="width: 30px; height: 30px; border-radius: 50%; object-fit: cover;">
                                    <span><%= member.name %></span>
                                    <span style="color: var(--gold); font-size: 0.8em; margin-left: auto;"><%= member.rank %> &rarr;</span>
                                </a>
                            </li>
                        <% }) %>
                    </ul>

                </div>
            </div>
        </main>
    </div>
</body>
</html>